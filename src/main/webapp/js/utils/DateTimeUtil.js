//flow
import * as _ from "lodash";

/**
 *
 */
export default class DateTimeUtil {
  /**
   * Returns the current time in milliseconds.
   * @return {number}
   */
  static now() {
    return _.now();
  }

  
}